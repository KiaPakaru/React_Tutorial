import React from "react";

class Footer extends React.Component {
  render() {
    return (
      <div>
        <p>Click the button below to add a new Item to the list.</p>
      </div>
    );
  }
}

export default Footer;
